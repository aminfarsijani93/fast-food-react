import React, { Component } from 'react'

export default class Login extends Component {
    render() {
        return (
            <div className="login container">
                <button className="btn btn-info">ورود / ثبت نام</button>
            </div>
        )
    }
}
